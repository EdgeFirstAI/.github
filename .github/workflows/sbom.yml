name: sbom

on:
  workflow_call:
    inputs:
      mode:
        description: dependency or full
        type: string
        default: dependency
      project-name:
        type: string
        default: ${{ github.event.repository.name }}
      project-type:
        type: string
        default: library
      version-file:
        type: string
        default: Cargo.toml
      source-dirs:
        type: string
        default: src tests crates
      runner:
        type: string
        default: ubuntu-24.04
      timeout-minutes:
        type: number
        default: 20
      upload-name:
        type: string
        default: sbom

jobs:
  sbom:
    name: SBOM (${{ inputs.mode }})
    runs-on: ${{ inputs.runner }}
    timeout-minutes: ${{ fromJSON(inputs.timeout-minutes) }}
    permissions:
      contents: read
    steps:
      - name: Checkout caller
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 1

      - name: Checkout shared CI
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          repository: EdgeFirstAI/.github
          ref: ${{ github.workflow_sha }}
          path: .ef-ci
          persist-credentials: false

      - name: Setup Rust
        if: hashFiles('Cargo.toml') != ''
        uses: ./.ef-ci/.github/actions/setup-rust
        with:
          components: rustfmt
          tools: cargo-cyclonedx
          cache-key: sbom-${{ inputs.mode }}

      - name: SBOM + license policy
        uses: ./.ef-ci/.github/actions/sbom-tools
        with:
          mode: ${{ inputs.mode }}
          project-name: ${{ inputs.project-name }}
          project-type: ${{ inputs.project-type }}
          version-file: ${{ inputs.version-file }}
          source-dirs: ${{ inputs.source-dirs }}
          upload-name: ${{ inputs.upload-name }}
