# Caller skeleton — SHA is the v1.0.0 workflow tree (merge of #4).

name: Nightly

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

jobs:
  changed:
    uses: EdgeFirstAI/.github/.github/workflows/nightly-gate.yml@22209a4a84fba9ffe1abec06df9ee43bf163561b  # unreleased shared-sha

  nightly:
    needs: changed
    if: needs.changed.outputs.run == 'true'
    uses: EdgeFirstAI/.github/.github/workflows/rust-full.yml@22209a4a84fba9ffe1abec06df9ee43bf163561b  # unreleased shared-sha
    with:
      lanes: all
      nightly: true
      runner-class-linux: hosted
      shared-sha: 22209a4a84fba9ffe1abec06df9ee43bf163561b
    secrets: inherit
